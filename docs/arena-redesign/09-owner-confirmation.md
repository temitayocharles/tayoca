# Tayoca Arena Redesign — Items Requiring Owner Confirmation

These items could not be fully verified from repository evidence or public sources. The redesign either excluded them, kept them with disclosure, or made conservative choices — each needs an owner decision before or after reconciliation.

## Claims & disclosure

1. **Withdrawn $216K AWS narrative** — `results.html` previously linked `blog/how-we-saved-216k-on-aws-in-90-days.html` as evidence while the homepage states the narrative is withdrawn from proof use pending re-verification. Redesign removed it from Results. Owner decision: (a) re-verify and restore as a documented case study, or (b) mark both $216K articles as "withdrawn narrative" notices on the articles themselves.
2. **About-page metric tiles** ("Deploy to prod ≤15 min", "MTTR ≤5 min", "Cloud bill ≤80% baseline") — removed; no traceable evidence found in repo. Restore only with an evidence ledger entry.
3. **Credential badges** (PMP, CSM, Terraform Associate, AWS Solutions Architect) — retained only on About (removed from footers). Confirm each is current/verifiable and keep an attestation record.
4. **SiteSupply** — presented on `/work.html` as a venture/build with the blog article as canonical source ("helping bring a B2B procurement marketplace to market… Canada and the United States"). Confirm: disclosure status (client build vs. Tayoca venture), naming rights, and whether a dedicated page is desired.
5. **Sivanta production status** — `data/sivanta-offers.md` mentions a sandboxed production deployment and a POG Technologies deployment as a portfolio/case-study candidate. The public `sivanta.html` does not name any tenant (correct). Confirm what may be published publicly about deployments before any case study is added.
6. **AI Made Simple** — landers exist (`landing_ai-made-simple.html`, `landing_ai-made-simple-page.html`) but the product is "staged" per `EPIC_checklist.md` and is not in the stage-10 catalogue. Kept out of products/portfolio. Confirm status (publish, keep staged, or archive landers).
7. **Legacy landers** — `/l/*` (Tailwind CDN) and superseded product landers remain live behind redirects. Recommend: archive `l/*` to noindex or remove once 301s are confirmed; keep redirects indefinitely.

## Control-plane

8. **Gateway path allowlist** — the Unified Gateway v6.1's exact `public/**` read/write allowlist lives in the runtime workflow, not the repo. Confirm the allowlist so `company-ecosystem.json` and `work.html` can be added to CMS-managed entities deliberately.
9. **Workflow registry freshness** — `workflow-registry.yaml` last reconciled 2026-08-11; confirm still current (5 active workflows) before the rationalisation plan is executed.
10. **Products feed** — confirm whether `products-feed.csv` is generated by a workflow (format contract) or hand-maintained, so the new registry can reference it correctly.

## Operating plan

11. This redesign is a public-surface + content-model change. Per `docs/production-growth-operating-plan.yaml`, post-certification work follows the locked plan; this branch is an external review workspace. Confirm the reconciliation path into canonical Forgejo and the intended cycle/wave for deployment.

## Discovery-scan items (added 2026-08-27 — from expanded GitHub ecosystem scan; see `10-company-discovery-inventory.md`)

12. **Zernio / ZernFlow** — public org `zernio-dev` (zernio.com): social & messaging API for developers and AI agents, visual flow builder (zernflow), SDKs, CLI, MCP servers, n8n nodes, unified inbox. The founder's account mirrors `zernflow` as a fork. Confirm: relationship to Tayoca (separate company? venture? collaboration?), and whether it may ever be referenced on tayoca.com. Not published pending this decision.
13. **9Drive** — public repo `zenhosta/9drive` (live 9drive.zenhosta.com), mirrored as a fork on the founder's account. Storage-gateway web app (multi-Drive dashboard, S3 endpoints, upload routing). Confirm: Tayoca product, personal venture, or zenhosta product; publication permission.
14. **zenhosta products** — `niagakita-digital`, `niagakita-toko-online` (Indonesian e-commerce, PHP), `zen-content-api` (WordPress JSON API). Confirm relationship and publication permission.
15. **SiteSupply ownership & evidence** — public repo `Emife1/sitesupply` + live sites (`sitesupply.cois.site` already linked from the Tayoca article; `sitesupply.vercel.app` in README). Confirm: owner of record (Tayoca vs Emife1 vs joint), what may be said about the live product (README marks workspace pricing/supplier-fit as demonstration data), and whether a dedicated portfolio page is wanted.
16. **Abiding Place Fellowship website** — public repo + live site (Shelburne, within the Free Website Initiative geography). Confirm: is this Tayoca community-initiative work or a personal/client build, and may Tayoca reference it? Church context: no reference without explicit consent.
17. **Hermes Agent workspace / Hermes cloud agent** — public but empty placeholder repos crediting the founder ("AI agent projects, research, and automation tools developed by @temitayocharles"). Confirm what Hermes is; nothing publishable exists yet.
18. **Creator Prompter** — named in the company brief; **no repository or public surface found** under any accessible account or global search. Confirm what it is, where it lives, and its disclosure status.
19. **Sivanta repository** — no repo found under accessible accounts (probed `sivanta*` names → 404); only the public page, offers doc and referenced demo URL. Confirm repo location/name and what may be published about deployments/tenants.
20. **n8n-control-plane repository** — referenced in `docs/final-certification/rollback-report.json`; probe → 404 under all scanned accounts. Confirm renamed/private/retired status.
21. **Control-console visibility** — `temitayocharles/tayoca-control-center` (public fork of `janmaaarc/n8n-ops`) and its authenticated preview deployment exist publicly. Confirm this is acceptable and that the deployment remains auth-gated (it must not be linked from tayoca.com as a public showcase).
22. **Live-runtime workflow reconciliation** — the repository workflow registry is a stale snapshot; the live n8n estate is materially broader. Confirm who executes the reconciliation and when (see `04` §6).
23. **Embedded authorization material remediation** — live inspection found authorization material embedded in workflow code. Confirm execution of the documented remediation (migrate → rotate → identify callers → migrate → test → rollback → certify; see `05` §5.2). This workspace holds no authority to change production n8n.
24. **Other private repositories** — the discovery token could not enumerate the full private estate; confirm whether other private projects exist that should remain private (default) or be referenced generically.

**Disclosure default applied:** none of the discovery-scan projects (12–17) were added to the public website. Only SiteSupply, which was already published via the blog article, remains on `/work.html` unchanged.
