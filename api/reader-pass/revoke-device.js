import {json,readJson,readSession,sql} from "./_lib/reader-pass.js";
export default async function handler(req,res){if(req.method!=="POST")return json(res,405,{error:"method_not_allowed"});try{const raw=(req.headers.cookie||"").match(/(?:^|; )tayoca_reader=([^;]+)/)?.[1],s=await readSession(raw||"");const b=await readJson(req);if(!b.device_hash)return json(res,400,{error:"invalid_request"});await sql("update reader_devices set revoked_at=now() where reader_pass_id=$1 and device_hash=$2",[s.sub,b.device_hash]);return json(res,200,{ok:true});}catch{return json(res,401,{error:"unauthorized"});}}